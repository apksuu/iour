name: 星奈的调试小助手～（远程连接）

on:
  workflow_dispatch:   # 手动点 Run workflow 哦～

jobs:
  debug:
    runs-on: ubuntu-latest
    timeout-minutes: 180   # 跑3小时够啦～

    steps:
    - uses: actions/checkout@v4

    - name: 🚀 启动 tmate 远程连接
      uses: mxschmitt/action-tmate@v3
      # 去掉限制，SSH 直接就能连～

    - name: 💤 保持会话一直开着
      run: sleep 10800
